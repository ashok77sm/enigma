pipeline {
   agent {label 'build'}
   parameters {
       choice choices: ['ashok', 'crimson', 'enigma'], name: 'user'
   }
   
      stages {
	      stage('checkout-stage') {
		       steps {
		          echo "This is checkout stage"
			     }  
		    }
		    stage('test-stage') {
                      when {
                         branch 'dev'
                         environment name:'user' value:'crimson'
		      }
		       input {
                       message 'Would you like to continue?'
                       ok 'Yes'
		       submitter 'ashok'
           }
			      steps { 
			        echo "This is test stage"
			      }  
		     }
	     }
}	 
